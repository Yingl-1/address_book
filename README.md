Chinses：
通讯录管理系统
使用结构体，结构体数组，指针，引用，函数（应该包含函数封装和接口？大概？好像？）的多文件项目。
仅用于致目前为止总结学过的知识点，无任何可用价值。
指针和引用我一直容易弄混，具体应该如何分辨，
例：指针：
		a（&p）;//调用
		void a(*p){};//定义
	引用：a（p）//调用
		void a（&p）{}//定义
			应该是这样的吧。？
如有优化空间，欢迎指点。

English：
Contact Management System
A multi-file project that utilizes structs, arrays of structs, pointers, references, and functions (likely including function encapsulation and interfaces—roughly speaking?). This project is intended solely as a summary of the concepts learned so far and has no practical value.
I’ve always tended to confuse pointers and references. How exactly can I tell them apart?
For example:
	Pointer:
		a(&p); // invocation
		void a(*p) {} // definition
	Reference:
		a(p); // invocation
		void a(&p) {} // definition
			Is this roughly correct.?
If there’s room for improvement, I’d welcome your guidance.